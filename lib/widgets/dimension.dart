import 'package:flutter/material.dart';

class Dimensions {
  Dimensions._();

  static BorderRadius defaultBorderRadius = BorderRadius.circular(10);
  static BorderRadius mediumBorderRadius = BorderRadius.circular(8);
  static BorderRadius largeBorderRadius = BorderRadius.circular(15);
  static BorderRadius smallBorderRadius = BorderRadius.circular(6);
  static BorderRadius tinyBorderRadius = BorderRadius.circular(3);

  static const double defaultPadding = 18.0;
  static const double mediumPadding = 16.0;
  static const double largePadding = 22.0;
  static const double smallPadding = 12.0;

  static const double defaultButtonHeight = 50;
  static const double defaultButtonWidth = double.infinity;
}
